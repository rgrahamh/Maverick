include "Entity.fbs"

table{
    name:string;
    global_x:long; //The global X coordinate of the level's origin
    global_y:long; //The global Y coordinate of the level's origin
    entities:[Entity] (required);
}